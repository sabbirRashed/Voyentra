import LoadingSpinner from '@/components/LoaderSpinner';
import React from 'react';

const loading = () => {
    return (
        <div>
            <LoadingSpinner/>
        </div>
    );
};

export default loading;